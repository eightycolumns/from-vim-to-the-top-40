#ifndef OPERATOR
#define OPERATOR # 1 #

giSinFt ftgenonce 0, 0, pow(2, 16), 10, 1

opcode Operator, a, kai
  kCps, aModulation, iInitPhase xin
  xout a(tablei(phasor(kCps) + aModulation, giSinFt, 1, iInitPhase, 1))
endop

#end
